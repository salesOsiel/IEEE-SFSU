# Website pictures

Place website picture files in this folder, then list them in `pictures.js`.

For example, if this folder contains `solar-gators.jpg`, use:

```js
image: "images/solar-gators.jpg"
```

Use lowercase file names without spaces when possible. JPG, PNG, and WebP files all work.
